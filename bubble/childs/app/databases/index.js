module.exports = function ($config, $interfaces, $databases, $libs, $parent) {
	// Dependencies
		var $ = {};
		$.config = $config;
		$.interfaces = $interfaces;

	// Databases
		// $databases.sequelize = require('./sequelize')($);

}